/**
 * Package version — keep in sync with package.json when bumping.
 * Used to populate User-Agent headers so the version string is maintained
 * in a single source-controlled location rather than scattered across providers.
 */
export const PKG_VERSION = "0.0.0"
export const PKG_NAME = "@pumpkinredbean/tiny-agent-runtime"
